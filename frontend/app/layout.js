import { Noto_Sans_Thai } from "next/font/google";
import "./globals.css"; // ไฟล์ CSS ที่จะใช้ทั่วทั้งโปรเจ็กต์
import Header from './components/Header.js';
import Footer from './components/Footer.js';

const notoSansThai = Noto_Sans_Thai({
  variable: "--font-noto-sans-thai", // ตัวแปรที่ใช้ใน CSS
  subsets: ["thai", "latin"], // รองรับภาษาไทยและภาษาอังกฤษ (ละติน)
  weight: ["400", "700"], // น้ำหนักตัวอักษรที่ต้องการ
});

export const metadata = {
  title: "REPAIR APP",
  description: "Generated by spwtt.d",
};




export default function RootLayout({ children }) {
  return (
    <html lang="en" className="h-full">
      <body className={`${notoSansThai.variable} antialiased min-h-screen flex flex-col`}>
        {/* <Header /> */}
        {children}
        {/* <Footer /> */}
      </body>
    </html>
  );
}